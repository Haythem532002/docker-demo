spring:
  datasource:
    url: jdbc:postgresql://localhost:1234/demo_db
    username: postgres
    password: haythem
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    database: postgresql

server:
  port: 1200
